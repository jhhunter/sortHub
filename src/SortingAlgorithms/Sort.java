package SortingAlgorithms;


/**
 * A super class for every sorting algorithm to inherit from.
 * Contains the method sort which will have a unique implementation for each subclass
 */
public class Sort {

    public int[] sort(int[] unsorted_list) {
        return new int[unsorted_list.length];
    }

}
